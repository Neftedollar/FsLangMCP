bin/
obj/
nupkg/
*.binlog
.claude/
tests/*/bin/
tests/*/obj/
coverage*/
tests/*/coverage*/
CoverletSourceRootsMapping_*
.runtime-tools/
.fslangmcp-live-format-*.fs
__pycache__/
*.py[cod]

# Verify (snapshot testing): *.received.* are produced when a snapshot diverges
# from its *.verified.* baseline. Reviewed locally, then either approved
# (renamed to .verified.*) or the test is fixed. Never committed.
*.received.*
